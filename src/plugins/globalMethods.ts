/**
 * 注册全局方法 待完善
 * @param app
 */
// import { getCurrentPage } from "@/utils/index";
export function setupGlobalMethods(app) {
  //   app.config.globalProperties.$getCurrentPage = getCurrentPage;
}
